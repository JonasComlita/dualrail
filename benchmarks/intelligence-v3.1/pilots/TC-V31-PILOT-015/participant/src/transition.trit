fn transition_state(state: t40, event: t40, aux: t40) -> t40 {
    var candidate: t40 = state;
    if event < 0 { candidate = state - aux - 2; }
    else {
        if event == 0 { candidate = state - 2; }
        else { candidate = state + aux; }
    }
    return normalize_value(candidate);
}
