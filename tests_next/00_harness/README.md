# 00 Harness

Shared harness code belongs here once tests are ported.

First tasks:

- define assertion helpers;
- define temp workspace helpers;
- define golden file lookup conventions;
- define deterministic random seed helpers;
- define VM/app boot helper boundaries without hiding test intent.
