# 01 ISA

Focused tests for ISA contracts.

Add one behavior per test case:

- opcode encoding;
- register fields;
- immediate decoding;
- branch encoding;
- CSR names;
- invalid instruction rejection;
- vector/lane instruction encodings.

Old reference tests: `tests/test_isa_asm.cpp`, `tests/test_vm_widths.cpp`.
