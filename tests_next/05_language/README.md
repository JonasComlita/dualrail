# 05 Language

Golden `.trit` source behavior tests.

Required areas:

- arithmetic and comparisons;
- `if`, `while`, and `match`;
- functions and recursion;
- structs, arrays, constants, and generics;
- ownership moves/drops;
- unsafe load/store and atomics;
- syscall-facing examples.
