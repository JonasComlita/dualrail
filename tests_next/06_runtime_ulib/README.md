# 06 Runtime Ulib

Tests for `ulib.trit`, `ulib_c.trit`, `ulib_hmap.trit`, `ulib_mini.trit`,
and app SDK wrappers.

Required areas:

- text output and formatting;
- parse/itoa helpers;
- malloc/free;
- collections;
- syscall wrapper IDs and status conventions;
- app/window/process helper wrappers.
