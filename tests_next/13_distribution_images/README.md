# 13 Distribution Images

Tests for Path A distribution artifacts.

Required areas:

- `.tboot/.tiso` manifest validation;
- `.tdisk` persistence;
- checksum and version failure cases;
- release image builder output;
- boot without source files present;
- host runtime diagnostic export.
