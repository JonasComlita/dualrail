---
{
  "schema": "trit.treatcode_learning_page.v2",
  "id": "low-level-ir-linking",
  "title": "IR and the linking boundary",
  "module": "Phase 07 · Low-level assembly IR and linking boundary",
  "level": "beginner",
  "order": 14,
  "summary": "Build the mental model for Low-level assembly IR and linking boundary.",
  "phase_id": "tc:layer:phase-07-low-level-ir",
  "phase_slug": "low-level-ir",
  "phase_name": "Low-level assembly IR and linking boundary",
  "lesson_kind": "mental-model",
  "implementation_status": "complete",
  "canonical_terms": [
    "intermediate representation",
    "lowering",
    "basic block",
    "linker",
    "symbol resolution"
  ],
  "objectives": [
    "Explain IR purpose, lowering, object and link stages, symbol resolution, and link failures.",
    "Distinguish the general concept from the current Trit implementation and planned gaps.",
    "Use a worked example and repository evidence to validate one claim."
  ],
  "prerequisites": [
    "assembler-relocation-lab"
  ],
  "sources": [
    {
      "path": "ternary_ir.h",
      "label": "ternary_ir.h",
      "kind": "source"
    },
    {
      "path": "docs/05_Compiler_Infra/ternary_ir.md",
      "label": "ternary_ir.md",
      "kind": "documentation"
    },
    {
      "path": "tests/test_ternary_ir.cpp",
      "label": "test_ternary_ir.cpp",
      "kind": "source"
    }
  ],
  "evidence": [
    {
      "path": "TEST_MANIFEST.json",
      "label": "Authoritative test manifest",
      "kind": "manifest"
    },
    {
      "path": "tests/test_ternary_ir.cpp",
      "label": "tc:test:test-ternary-ir test source",
      "kind": "test"
    }
  ],
  "test_ids": [
    "tc:test:test-ternary-ir"
  ],
  "benchmark_ids": [],
  "gap_ids": [],
  "stack_links": {
    "phase": "/stack/low-level-ir",
    "source": "https://github.com/JonasComlita/dualrail/blob/02f764a96b6a9ce98b3b63a07a0771b04d1a627d/ternary_ir.h",
    "tests": "/stack?focus=tc%3Atest%3Atest-ternary-ir"
  },
  "next": "link-failure-investigation",
  "interactive": {
    "kind": "trace",
    "title": "Low-level assembly IR and linking boundary trace",
    "prompt": "Advance through the low-level-ir transition and inspect the state at each boundary.",
    "steps": [
      {
        "label": "Input contract",
        "state": "typed or parsed operations above machine encoding",
        "explanation": "Start with typed or parsed operations above machine encoding."
      },
      {
        "label": "State or artifact check",
        "state": "Lower an addition function into a block with two input values, an add operation, and a return",
        "explanation": "Read the IR struct definitions and one test that verifies a node or operand."
      },
      {
        "label": "Boundary transition",
        "state": "a lower-level representation with explicit symbols and link diagnostics",
        "explanation": "The transition leaves the low-level-ir boundary only when its invariant holds."
      },
      {
        "label": "Observable result",
        "state": "Evidence: tc:test:test-ternary-ir",
        "explanation": "Use tc:test:test-ternary-ir to validate the observed result."
      }
    ]
  }
}
---

## Objectives

- Explain IR purpose, lowering, object and link stages, symbol resolution, and link failures in general computer-systems terms.
- Identify the current Trit implementation, its entry and exit artifacts, and its explicit status.
- Use a worked example and the linked interaction to make one falsifiable claim.
- Leave with a next step that keeps prerequisites, source, and validation visible.

## Prerequisites

Read [assembler-relocation-lab](/learn/eecs/assembler-relocation-lab) first. Those lessons introduce the state and vocabulary that this page assumes. This is the mental-model lesson for Low-level assembly IR and linking boundary. Begin with the general systems idea, then compare it with the current Trit boundary before you touch a tool. A prerequisite is a reasoning dependency, not a UI gate: a direct link to this page must still explain what is assumed.

## Why this topic exists

An intermediate representation gives a compiler a stable place to reason before committing to a binary layout. It can expose types, control flow, calling convention, and ownership more clearly than assembly, while still being close enough to lower into instructions. The linking boundary is where independently produced pieces meet; a good IR and object model make a missing symbol or incompatible relocation visible rather than hiding it in generated bytes. In a real system, the boundary exists because two parts need to cooperate without sharing every implementation detail. It makes a failure local enough to diagnose and a change narrow enough to review. The useful question here is not only “what does Trit call this?” but “what invariant would another computer system need at the same boundary?”

## Explanation

ternary_ir.h and the compiler-infrastructure documentation describe the current IR shape. The IR tests are the direct evidence for construction and invariants. The linker boundary is intentionally read alongside assembler contracts: lowering can be correct while a later symbol or object error still prevents a usable program. The phase enters through typed or parsed operations above machine encoding and leaves through a lower-level representation with explicit symbols and link diagnostics. The distinction between the ideal concept and the repository implementation matters: a textbook may describe a complete mechanism, while the current code may implement a bounded slice, a host adapter, or an explicit stub. TreatCode's labels are therefore part of the explanation. “Implemented” means the cited source and validation support a current behavior; “planned” means the roadmap or gap records a future behavior; “unavailable” means the evidence or required asset is not present.

Lower an addition function into a block with two input values, an add operation, and a return. The IR names the values before registers exist. Register allocation later maps them to concrete locations, while linking resolves the function symbol. If the return type and call-site ABI disagree, the failure belongs at the interface even if the add operation itself is valid. The same reasoning scales beyond the example. Name the input, the state that changes, the owner of that state, the output, and the test that would catch a regression. If one of those is missing, say so. A source link can help a reader continue, but it cannot substitute for the prose that explains why the source matters.

## Current implementation and planned work

ternary_ir.h and the compiler-infrastructure documentation describe the current IR shape. The IR tests are the direct evidence for construction and invariants. The linker boundary is intentionally read alongside assembler contracts: lowering can be correct while a later symbol or object error still prevents a usable program. The current registry row is tc:layer:phase-07-low-level-ir, with implementation coverage marked complete and tested coverage marked complete. The lesson is anchored to the current public snapshot; a later snapshot may change paths or statuses and must be regenerated rather than silently inferred.

Some richer optimization and debug metadata may be future work. The current lesson treats IR fields present in source and tests as implemented, and names absent stages as planned rather than implying a full production linker. Keep the wording scoped. A planned driver, missing benchmark asset, or unavailable hardware trace is valuable information for a contributor because it describes the next evidence needed. It is not a failure of the concept, and it is not permission to call a partial path complete.

## Worked example

Lower an addition function into a block with two input values, an add operation, and a return. The IR names the values before registers exist. Register allocation later maps them to concrete locations, while linking resolves the function symbol. If the return type and call-site ABI disagree, the failure belongs at the interface even if the add operation itself is valid. For this lesson, write the example as a sequence: first identify typed or parsed operations above machine encoding; next apply the low-level-ir rule; then inspect a lower-level representation with explicit symbols and link diagnostics; finally compare the result with the named validation record. The conceptual check should explain why the result would be wrong if a neighboring representation or layer were substituted. This sequence is deliberately small enough to execute or trace, yet concrete enough to expose a wrong assumption.

## Common misconception

IR is not an executable substitute for a linked image. It is a compiler contract whose usefulness comes from preserving meaning between stages. A pretty IR dump cannot prove that code generation, object emission, and link resolution all succeed. Another common shortcut is to treat a green UI response as proof that the underlying compiler, VM, kernel, or device completed the work. The interaction on this page names its limits and points back to the exact source and test records. When the result is unavailable, the correct learner response is to report the limitation and preserve the evidence trail.

## Learner action

Write the same three-operation function as source, IR, and a symbolic assembly sketch. Circle the first point where an address becomes concrete and name the test that should cover that transition. Record your result in four sentences: what you expected, what state or artifact you inspected, which source/test evidence supports it, and what remains uncertain. If you are working in the code exercise, keep the program bounded and observe the returned compiler or VM result. If you are working in the trace, advance one state at a time and do not skip the invariant. This action turns reading into a reproducible investigation that another learner can review.

## Source links

- [ternary_ir.h](https://github.com/JonasComlita/dualrail/blob/02f764a96b6a9ce98b3b63a07a0771b04d1a627d/ternary_ir.h) — ternary_ir.h
- [ternary_ir.md](https://github.com/JonasComlita/dualrail/blob/02f764a96b6a9ce98b3b63a07a0771b04d1a627d/docs/05_Compiler_Infra/ternary_ir.md) — docs/05_Compiler_Infra/ternary_ir.md
- [test_ternary_ir.cpp](https://github.com/JonasComlita/dualrail/blob/02f764a96b6a9ce98b3b63a07a0771b04d1a627d/tests/test_ternary_ir.cpp) — tests/test_ternary_ir.cpp

These are authoritative repository references for the claim. The [Stack Explorer phase](/stack/low-level-ir) provides the full relationship view, including related components, contracts, decisions, tests, benchmarks, releases, and gaps.

## Validation and evidence

- [Authoritative test manifest](https://github.com/JonasComlita/dualrail/blob/02f764a96b6a9ce98b3b63a07a0771b04d1a627d/TEST_MANIFEST.json) — TEST_MANIFEST.json
- [tc:test:test-ternary-ir test source](https://github.com/JonasComlita/dualrail/blob/02f764a96b6a9ce98b3b63a07a0771b04d1a627d/tests/test_ternary_ir.cpp) — tests/test_ternary_ir.cpp

The test IDs attached to this lesson are tc:test:test-ternary-ir. Run the smallest focused check first, then the broader suite named by the plan. Evidence is commit-addressed and may be marked active, referenced, planned, or unavailable. Read the status before repeating the conclusion. This phase has no benchmark record in the current registry.

## Next step

[Continue to the next lesson](/learn/eecs/link-failure-investigation). The next page should make the dependency explicit and keep the same distinction between general concept, current Trit behavior, and planned work. When you reach the terminal lesson, write the closure report and list residual gaps instead of assuming that navigation itself proves completion.
